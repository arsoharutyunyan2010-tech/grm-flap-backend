# GRM FLAP — Անվտանգության աուդիտ և գնահատական

Աուդիտի ամսաթիվ` 2026-09-06
Վերանայված ֆայլեր` `server.js`, `anticheat.js`, `physics.js`, `telegramAuth.js`,
`store.js`, `rewards.js`, `bot.js`, `bots.js`, `index.html` (կարևոր հատվածները),
`admin.html`, `env.example.txt`:

---

## Ընդհանուր գնահատական

| Ոլորտ | Գնահատական | Մեկնաբանություն |
|---|---|---|
| **Միավորը սկրիպտով «չավելացնել»** (score faking) | 9/10 | Շատ լավ: միավորը երբեք չի հաշվվում հաճախորդի կողմից |
| **Սեսիայի վերարտադրում (replay)** | 8.5/10 | Seed+flapLog+heartbeat` ճիշտ է կառուցված |
| **Telegram auth-ի ամրություն** | 8.5/10 | Բոլոր /api-ում, timingSafeEqual, 12ժ ժամկետ |
| **Փող/մնացորդ (deposit/withdraw/referral)** | 6/10 | Հիմնականում ձեռքով ադմին ստուգում, on-chain վավերացում չկա |
| **Բոտ-հաշիվների / մուլտի-ակաունթ ֆարմ** | 5.5/10 | Telegram-ի կրկնօրինակ initData` դժվար է բռնել |
| **Վարչական անվտանգություն (ADMIN_KEY)** | 6/10 | Միակ բանալին, ամբողջական մուտք, 2FA/IP-allowlist չկա |
| **Կարգավորման / գաղտնիքների հիգիենա** | 8.5/10 | .env-ն ու store.json-ը դուրս են git-ից, repo չի արտահոսվում |

**Ընդհանուր` 7.5 / 10** — ամուր սկզբունք, ապացուցված թեստերով, բայց մի քանի
իրական «մարդ-գործոնով» խարդախության ճանապարհ դեռ բաց է: Կոդի մեջ **ոչ մի հայտնի,
անմիջական ակտիվ-հարձակում** (injection, path traversal, auth bypass, IDOR) չգտա:

---

## 1) Ինչ արդեն ճիշտ ու ամուր է արված

- **Միավորը հաշվում է միայն սերվերը:** Հաճախորդն ուղարկում է raw inputs
  (`flapLog`, `totalSteps`, session)` իսկ սերվերը նույն `physics.js`-ով (նույն
  seed-ով) ամբողջը կրկին «խաղում է»: `clientScore`-ն ընդհանրապես չի հաշվվում` միայն
  mismatch-ի ազդանշան է: Դրա շնորհիվ devtools-ում `submitScore(999999)` գրելն ուղղակի
  **անհնար է**:
- **Միանվագ session (one-shot):** session-ը consume է լինում մինչև simulate-ը, հետևաբար
  captured հարցումը կրկին չի կարող գրանցվել:
- **Timing walls:** հնարավոր steps-ը սահմանափակվում է **իրական** սեսիայից անցած
  ժամանակով (`allowedStepsFor`), այնպես որ «հին 10 րոպեանոց կատարյալ run» անմիջապես
  արհեստական հնարավորություն չկա:
- **Heartbeat-ներ` աճող step-ով,** որպեսզի ապացուցվի, որ խաղացողը «նստած» է եղել
  run-ի մեջ, ու step-ի մեծ թռիչքը բռնվում է:
- **Flap-կադենսի վերլուծություն** (մետրոնոմ/ռեգուլյար/մին-ինտերվալ բոտերի դեմ):
- **Telegram initData-ի իսկություն** ամեն մուտքի և ֆինանսական գործողության վրա,
  timing-safe համեմատությամբ:
- **HMAC session token**` կապված session+userId+seed-ին:
- **Revive-ները չեն տրվում start-ում** (ad-ը սերվերային S2S չի հաստատվում, ուստի
  revive-ներն էլ չեն կարող մինտվել):
- **Սահմանափակումներ և արգելքներ (strikes → temp-ban)**` 8+ հարված` 48ժ:
- **IP-throttling, 429,** կոպիտ և նուրբ շերտերով:
- **Գաղտնիքներ չեն արտահոսում.** `.env`, `data/`, `store.json` git-ից ու Docker
  image-ից դուրս: Ցանկացած չգրված ճանապարհ 404 է (նախկինում ամբողջ repo-ն էր արտահոսում):
- Թեստերը (`anticheat`, `persistence`) կանաչ են:

---

## 2) Ինչ խարդախություն կարող են փորձել սկրիպտով / չիտով / userscript-ով

Ստորև` **իրական** փորձի սցենարներ, դասավորված` ըստ ռեալիզմի ու ազդեցության:

### A. Իրական ժամանակի ավտո-բոտ (ամենառեալիստիկ միավորային խաբեությունը)
- Ինչպես է աշխատում. չիտն իջեցնում է `physics.js`-ը (այն հրապարակային `/physics.js`),
  բացում իրական session, և **իրական ժամանակի** ընթացքում (համապատասխանեցնելով
  heartbeat-ի 3վ արագությանը) լուծում է seed-ի համար գրեթե-օպտիմալ flapLog:
  Քանի որ սիմուլյացիան որոշակի է (deterministic), այն «գիտի»` երբ ֆլեյփել։
- Սերվերի պաշտպանությունը` flap-կադենսի հևրիստիկան (մին-ինտերվալ, ցածր variance,
  մետրոնոմ): Սակայն **լավ բոտը, որ պատահական jitter է ներարկում (10–11% տատանում)
  և ոչ մշտական ինտերվալով է ֆլեյփում, կարող է խուսափել** այս հևրիստիկաներից:
- Ինչու է կարևոր. weekly reward-ը (GRM → withdrawable FLAP) տրվում է վերևի ~10
  լիդերին, ուստի բարձր միավոր = փող:
- **Մեղմացում.** (ա) ավելի «անկանխատեսելի»/դժվար լուծելի pipe-տրամաբանություն,
  (բ) պատահական ռեակցիա-միջոցառումներ (reaction events) որոնք բոտը չգիտի,
  (գ) ավելի խելացի վիճակագրական անալիզ` ֆլեփի ժամանակամիջոցների **բաշխման**
  (entropy/permutation) վրա` ոչ միայն mean/stdev,
  (դ) ակաունթի/սարքի մակարդակի արագություն ու սահմանափակում:

### B. Բոտ-հաշիվների / մուլտի-ակաունթ ֆարմ + ինքնահղում
- Ինչպես է աշխատում. մեկ մարդ վերահսկում է շատ իրական (կամ էժան գնված) Telegram
  հաշիվներ, յուրաքանչյուրը `startapp=ref_X`-ով բացում է, որ X-ը ստանա invite-count
  ու daily referral-լիդերբորդի տեղ. Բացի այդ, եթե ադմին հաստատում է deposit-ներ, ապա
  «սեփական» ալտ-հաշվի իրական deposit-ի դիմաց X-ը ստանում է 7%+3%+1% «կոմիսիա»:
- Ինչու է աշխատում. `verifyInitData`-ն ապացուցում է, որ *հարցումը գալիս է Telegram-ի
  այդ հաշվից*, բայց **ոչ** որ դա *տարբեր մարդ է*: Ով շատ TG հաշիվ ունի, նա շատ «օրինական»
  initData ունի:
- **Մեղմացում.** deposit/withdraw-ի վրա per-IP/per-device clustering, նոր հաշվի
  cool-down մինչև ֆինանսական գործողությունը, կոմիսիայի սահման (cap), withdraw-ի
  KYC/whitelist, referral-կոմիսիա` միայն «ապացուցված» ակտիվությունից հետո:

### C. Կեղծ / կրկնվող deposit հայտ (on-chain ստուգման բացակայություն)
- Ինչպես է աշխատում. deposit-ի ժամանակ հաճախորդն ուղարկում է `txHash` (կամ TON BOC):
  Սերվերը **միայն** format-ն է ստուգում (`sanitizeTxHash`) ու `BOC`-ի hash-ն է
  հաշվում. **այն չի հաստատում**, որ BOC/տրանզակցիան իսկապես deposit-ի հասցեին,
  ճիշտ գումարով է (չի հարցնում toncenter/tonapi-ին):
- Հետևանք. իմաստուն հարձակվողը կարող է ուղարկել ցանկացած **վավեր ստորագրված** TON
  transfer (մեկ ուրիշ հասցեի / աննշան գումարով)` իր BOC-ը որպես deposit ներկայացնելով:
  Փրկարարն այն է, որ ադմինը **ձեռքով** պետք է հաստատի; բայց դա դանդաղ է ու
  մարդկային սխալի տեղ է թողնում: Բացի այդ, `rejected` hash-ը կարելի է վերաներկայացնել:
- **Մեղմացում.** սերվերային on-chain վավերացում (toncenter/tonapi)` destination
  հասցեն, գումարը, (ցանկալի)` նշումը. Auto-approve` միայն համընկնելու դեպքում:

### D. Stolen / արտահոսած initData-ի օգտագործում (bearer-կրեդենցիալ)
- Ինչպես է աշխատում. initData-ն 12 ժամվա «bearer» անցաթուղթ է: Եթե այն արտահոսի
  (log-եր, screenshot, phishing, XSS), ապա հարձակվողը կարող է այդ 12 ժամվա ընթացքում
  հարձակվողի **անունից** սեսիաներ ստեղծել ու game-բոտ աշխատեցնել (կամ PvP-ում
  դիտավորյալ պարտվել / շահել):
- **Մեղմացում.** (ա) initData-ն կրճատել ֆինանսական գործողությունների համար
  (շատ ավելի կարճ ժամկետ/refresh), (բ) session-ը կապել սարք-նոնսի (device nonce)-ին,
  որը սերվերն առաջին անգամ տրամադրում է, (գ) կարևոր գործողությունների համար
  «նորից հաստատում» (re-auth):

### E. Session/հաշվի տիրացում չարամիտ admin-key-ով (մարդ-գործոն)
- Ինչպես է աշխատում. ամբողջ /internal-ը (ներառյալ **backup restore**, որը կարող է
  **ամբողջ store-ն վերնագրել, այդ թվում բոլոր balances/միավորները**) պաշտպանված է
  **միակ** `ADMIN_KEY`-ով, որը փոխանցվում է header-ով ու admin.html-ում localStorage-ում:
- Հետևանք. եթե բանալին արտահոսի (անզգույշ պահում, XSS, phishing)` ամբողջական մուտք,
  կարող է ստեղծել ցանկացած մնացորդ/միավոր:
- **Մեղմացում.** admin-ի համար IP-allowlist կամ 2FA, և backup-restore-ի համար առանձին
  (ավելի ուժեղ) բանալի, admin.html-ի access-log:

### F. Բանալի/հաշիվ որոնում (brute-force)
- requireAdmin-ն 8 փորձ/10ր/IP սահման է դնում. սա լավ է, բայց `ADMIN_KEY`-ը պետք է
  **շատ երկար** լինի (16+ արդեն պարտադիր է) և `SESSION_SECRET`-ը` առանձին:

### G. Սոցիալական / արտադրական սխալներ (ոչ կոդ)
- `ALLOW_INSECURE_DEV=true` ցանկացած հարցում առանց initData-ի որպես `dev-user`
  ընդունում է: Նաև `npm run dev`-ը ակտիվացնում է այն: Եթե ինչ-որ մեկը զարգացման
  ռեժիմը թողնի հրապարակային` ամբողջ ֆունկցիոնալությունը բաց է:

---

## 3) Ինչ «չի կարող» անել սկրիպտը/չիտը այս կառուցվածքում (կարևոր է հասկանալ)

- Բրաուզեր-կոնսոլում կամ userscript-ով **«իմ սկորը 9999»-ը ուղարկելը** չի աշխատի` սերվերը
  վերաելի ինքն է հաշվարկում:
- **Captured հարցումը նորից ուղարկելը** միավոր չի ավելացնի` session-ը one-shot է:
- **Revive-ներ մինթելը** չի աշխատի` դրանք start-ում չեն տրվում, և replay-ն դատարկ
  reviveLog-ով է:
- **Միավորների/balances ֆայլի ուղիղ խմբագրումը** (երբ store.json-ը սերվերի վրա է)` արտաքին
  օգտագործողի հասանելիությունից դուրս է:
- **Մեկ ուրիշ խաղացողի sessionId-ով միավոր գրանցելը**` արգելվում է (foreign session):
- Ֆլեփի միջակայքը գերազանցելը / «գերմարդկային» ռիթմը` բռնվում է:

Այսինքն` «սկրիպտով ինչ-որ բան ավելացնել/փոխել»-ի **ուղիղ** (direct) ճանապարհը կոդում
**փակված է**: Մնացածը վերը նկարագրված անուղղակի/մարդ-գործոնի ուղիներն են (A–G):

---

## 4) Թույլ կետերն ու ռիսկերը` առաջնահերթությամբ (P1 → P3)

### P1 (անել շուտ)
1. **On-chain deposit-ի վավերացում** (destination + amount)` toncenter/tonapi-ով.
   Auto-approve միայն վավերի դեպքում: Սա մեծագույն «ուղղակի» ֆինանսական ռիսկն է:
2. **ADMIN_KEY-ի պաշտպանություն.** Առնվազն` IP-allowlist/2FA; backup-restore` առանձին
   բանալի: Հաշվի առնել admin.html-ը, որը հրապարակային է:
3. **initData-ի ժամկետի կրճատում ֆինանսական գործողությունների համար** և/կամ
   device-nonce` session-ը «վերգող» անձին կապելու համար:

### P2 (կարճ ժամկետ)
4. **Game-բոտի ավելի խելացի դետեկտոր.** Ընթացիկն ընդամենը variance/մին-ինտերվալ/մետրոնոմ է:
   Ավելացնել` ֆլեփ-ինտերվալների **բաշխման entropy/permutation-անալիզ**, per-account
   միավոր-արագության (velocity) սահման, նոր-հաշվի «տաքացման» շրջան մինչև high-score
   ընդունելը:
5. **PvP / խոշոր stakes-ի հավելյալ ստուգում** (առանց bot-ի դեմ նույն հսկողության):
6. **Rejected deposit hash-ի կրկնաօգտագործումը սահմանափակել** (k + ժամանակ):

### P3 (բարելավումներ)
7. `SESSION_SECRET`-ը` BOT_TOKEN-ից առանձին ստեղծել ու չփոխանցել README/օրինակում:
8. Referral-կոմիսիա` ակտիվության/ժամանակի շեմով (anti-self-farm):
9. Admin.html-ի access-log ու յուրաքանչյուր /internal restore-ի audit-entry:

---

## 5) Առաջարկվող «անհապաղ» քայլերի ցուցակ (10–20 րոպեանոց)

- [ ] Համոզվել, որ արտադրության մեջ `NODE_ENV=production` և `ALLOW_INSECURE_DEV=false`:
- [ ] `ADMIN_KEY`-ը` շատ երկար պատահական տող (և ոչ կանխատեսելի):
- [ ] `SESSION_SECRET`-ը` առանձին երկար պատահական տող (ոչ BOT_TOKEN-ից կախված):
- [ ] Հաստատել, որ server.js-ն **իրական** reverse-proxy-ի ետևում է (Railway), որպեսզի
      `trust proxy: 1`-ը ճիշտ աշխատի (հակառակ դեպքում IP-ը կեղծելի է):
- [ ] Deposit-ի յուրաքանչյուր հայտ` ձեռքով + **on-chain** հաստատում` նախքան approve:
- [ ] Թողնել `/internal`-ը բրաուզեր-հասանելիությունից սահմանափակ (allowlist):

> Ծանոթություն. եթե ուզում եք, այս զեկույցում նշված P1/P2 բարելավումներից մի քանիսը
> (on-chain deposit check, bot-դետեկտորի ուժեղացում, admin-key 2FA/IP-gate,
> initData-ի կրճատված ժամկետ ֆինանսական գործողությունների համար)` կարող եմ
> **իրականացնել կոդում**` գոյություն ունեցող թեստերը պահելով կանաչ:

---

## Հավելված — իրականացված ամրապնդումներ (այս branch-ում)

Հետևյալ P1/P2 առաջարկները **իրականացվել են կոդում**` առկա թեստերը պահված կանաչ
(83 PASS / 0 FAIL, ներառյալ նոր `npm run test:security`):

1. **Ավտո-բոտ / seed-լուծող** — `anticheat.js`-ում ավելացվեց «machine-like tap
   concentration» դետեկտոր. երկար բարձր run-ի ֆլեփ-ինտերվալների բաշխումը, որը
   մետրոնոմային է` հազվադեպ «jitter»-ով, այժմ բռնվում է, ոչ միայն մաքուր
   մետրոնոմը (զրոյական-կեղծ-ազդանշանից խուսափելու համար` բարձր շեմերով):

2. **Մուլտի-ակաունթ / ինքնահղում** — invite-ն հիմա **հաշվվում է միայն իրական,
   վավերացված run-ից հետո** (`store.activateReferral`, կանչվում է submit-score /
   PvP-submit-ում): Ավելի վաղ` ընդամենը referral հղումը բացելը (դատարկ ալտ-հաշիվով)
   արդեն ավելացնում էր upline-ի invite-ն ու daily-լիդերբորդի միավորը:

3. **Կեղծ deposit** — նոր `verifyDeposit.js`. Երբ `DEPOSIT_ONCHAIN_ENABLED=true`
   (և `DEPOSIT_TON_ADDRESS`-ը դրված է), `/internal/deposits/:id/approve`-ը **չի
   մուտքագրում** հաշիվը, քանի դեռ հայտարարված txHash-ով իրական inbound TON
   փոխանցում չի գտնվել դեպի deposit-ի հասցեն: Անջատված վիճակում` նախկին ձեռքով
   հաստատման հոսքն անփոփոխ է:

4. **Արտահոսած initData (12ժ)** — ֆինանսական / PvP-stake կանչերի համար
   (`/api/withdraw`, `/api/deposit`, `/api/pvp/join`) ավելացավ
   `authenticateFresh()`` ավելի նեղ initData-«թարմություն» պատուհանով (default 20ր,
   կարգավորվում է `SENSITIVE_AUTH_MAX_AGE_SECONDS`): հին գողացված token-ով այլևս
   փող չի կարելի շարժել:

5. **ADMIN_KEY` մեկ բանալի` ամբողջական մուտք** —
   - առանձին `RESTORE_KEY` (կամընտիր)` միայն wholesale-restore endpoints-ների
     համար (`POST /internal/backup` ֆայլով, `POST /internal/backups/restore`);
   - կամընտիր `ADMIN_IP_ALLOWLIST` (comma)` IP-մակարդակի սահմանափակում admin-
     տարածքի համար` նախքան բանալին ստուգելը;
   - ամեն admin-հասանելիություն աուդիտի է վերցվում (`adminAudit`), ցուցադրվում է
     `/internal/stats`-ում;
   - `admin.html`-ը ձեռքով restore-ի դաշտ է ավելացրել` `x-restore-key` ուղարկելու
     համար, երբ դրված է:

### Նոր կարգավորումներ (տե՛ս `env.example.txt`)
```
RESTORE_KEY=
ADMIN_IP_ALLOWLIST=
SENSITIVE_AUTH_MAX_AGE_SECONDS=1200
DEPOSIT_ONCHAIN_ENABLED=false
TON_RPC_URL=https://toncenter.com/api/v2
TON_API_KEY=
```
> Ծանոթություն` on-chain deposit-ի ստուգումը default-ով **անջատ է**, որ հին
> deployment-ը չկոտրվի: Միացնելուց առաջ փորձեք մեկ իրական deposit-ի վրա և
> հարմարեցրեք `TON_RPC_URL`-ը` ըստ ձեր TON indexer-ի (toncenter/tonapi):
