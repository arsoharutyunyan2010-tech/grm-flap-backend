# Telegram bot token from @BotFather — required to verify Mini App initData
# AND to check that players joined @GRMFLAP and @GRMFLAPCHAT (getChatMember).
# The bot MUST be an administrator of both chats, or the check will fail.
BOT_TOKEN=123456:ABC-your-bot-token

# TASKS ("ЗАДАНИЯ") page — subscribe to the game's own Telegram channel & chat.
# TASKS_CHANNEL / TASKS_CHAT = the VERIFICATION target: a public @username
# (or t.me link) OR a numeric chat id (-100...). Leave TASKS_CHAT empty to
# hide the chat task. The bot MUST be an administrator of both, otherwise
# getChatMember cannot verify membership and CHECK always returns
# "verification unavailable".
# TASKS_CHANNEL_URL / TASKS_CHAT_URL = what the OPEN button opens; derived
# from the username automatically. For a PRIVATE chat (t.me/+invite link) you
# cannot verify by the link — set TASKS_CHAT to the numeric chat id (forward
# any message from the chat to @userinfobot to get it) and put the invite
# link into TASKS_CHAT_URL.
# TASKS_REWARD_FLAP = one-time FLAP bonus per completed task (0 = no reward).
TASKS_CHANNEL=@FFLAPY
TASKS_CHAT=
TASKS_CHANNEL_URL=
TASKS_CHAT_URL=https://t.me/+ONJCLQ91EwlmYTcy
TASKS_REWARD_FLAP=1

# HTTPS URL of index.html — used by the bot's Play button
MINI_APP_URL=https://grm-flap-backend-production.up.railway.app/index.html

# Set to "true" only on your local machine to test without launching
# through real Telegram (bypasses initData verification). NEVER set this in production.
ALLOW_INSECURE_DEV=false

# Secret key required in the x-admin-key header for EVERY /internal route.
# Use a long random value (at least 32 chars recommended). NEVER commit the real value.
ADMIN_KEY=generate-a-long-random-secret-here

# Public HTTPS base URL of this backend.
PUBLIC_URL=https://grm-flap-backend-production.up.railway.app

# Global per-IP request throttle.
GLOBAL_IP_RATE_PER_MIN=480

# REQUIRED in production: independent HMAC secret for one-shot session tokens.
# Do not reuse BOT_TOKEN. Generate a separate random secret of at least 32 chars.
SESSION_SECRET=generate-a-separate-random-session-secret-here

# Separate key for full-store restore/import operations.
# Strongly recommended in production and should differ from ADMIN_KEY.
RESTORE_KEY=generate-a-separate-random-restore-secret-here

# Optional allow-list of source IPs allowed to reach /internal routes.
# ADMIN_IP_ALLOWLIST=203.0.113.10,loopback

# Fresh Telegram initData required for money-moving calls (seconds).
SENSITIVE_AUTH_MAX_AGE_SECONDS=1200

# ON-CHAIN DEPOSIT VERIFICATION.
# Required for automatic crediting: every top-up is checked against the chain.
DEPOSIT_ONCHAIN_ENABLED=true
# Primary indexer. Get a free key at https://toncenter.com — the keyless public
# endpoint rate-limits, and a throttled lookup delays (never loses) a payment.
TON_RPC_URL=https://toncenter.com/api/v2
TON_API_KEY=
# Fallback indexer, used automatically when the primary one errors, rate-limits
# or answers with nothing. Optional Bearer token raises its limits.
TONAPI_URL=https://tonapi.io/v2
TONAPI_KEY=
MIN_DEPOSIT_NANO_TON=10000000

# AUTOMATIC TOP-UPS — keep this ON.
# The server watches the chain and credits the player's C balance by itself:
#   * a payment made with PAY WITH CONNECTED WALLET (signed BOC),
#   * a payment made straight from a wallet already VERIFIED on the account,
#   * a manual request with a pasted hash or explorer link.
# Nothing is credited without a real inbound transfer to DEPOSIT_TON_ADDRESS.
# Set to false only to force every top-up through admin.html by hand.
DEPOSIT_AUTO_CREDIT=true
DEPOSIT_AUTO_CREDIT_WINDOW_MS=600000
DEPOSIT_AUTO_CREDIT_DELAYS_MS=8000,12000,20000,30000,45000,60000,90000,120000,150000
DEPOSIT_AUTO_CREDIT_SWEEP_MS=30000
DEPOSIT_AUTO_CREDIT_TOLERANCE=0.12
TON_USD_PRICE=

# Attached-wallet scan: credits a transfer from a VERIFIED wallet even when the
# player never opened the top-up form. Only wallets that signed a transaction
# are eligible — a self-reported address is never enough.
DEPOSIT_LINKED_SCAN=true
DEPOSIT_LINKED_SCAN_MS=60000
DEPOSIT_LINKED_SCAN_FIRST_MS=15000
DEPOSIT_LINKED_SCAN_LOOKBACK_MS=1800000
DEPOSIT_LINKED_SCAN_LIMIT=50

# NEW-ACCOUNT SCORE GATE.
NEW_ACCOUNT_RUNS=5
NEW_ACCOUNT_SCORE_CAP=120

# Minimum FLAP coins per withdrawal request.
MIN_WITHDRAW_FLAP=10

PORT=3000

# TON address players send funds to when topping up FLAP.
DEPOSIT_TON_ADDRESS=UQB...your-ton-address

# Persistent storage. Prefer Upstash Redis in production.
DATA_FILE=/data/store.json
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token

# Data safety / rolling backups.
BACKUP_EVERY_MS=3600000
BACKUP_KEEP=48
BACKUP_TTL_SEC=2592000
AUTOSAVE_MS=300000
SHRINK_GUARD_RATIO=0.5
