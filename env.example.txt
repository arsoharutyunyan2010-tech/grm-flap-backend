# Telegram bot token from @BotFather — required to verify Mini App initData
# AND to check that players joined @GRMFLAP and @GRMFLAPCHAT (getChatMember).
# The bot MUST be an administrator of both chats, or the check will fail.
BOT_TOKEN=123456:ABC-your-bot-token

# HTTPS URL of index.html — used by the bot's Play button
MINI_APP_URL=https://grm-flap-backend-production.up.railway.app/index.html

# Set to "true" only on your local machine to test without launching
# through real Telegram (bypasses initData verification). NEVER set
# this in production.
ALLOW_INSECURE_DEV=false

# Secret key required in the x-admin-key header for EVERY /internal route
# (stats, deposits/withdrawals approvals, backups, bans, weekly rewards).
# Generate a long random string (16+ chars). If this is missing/short, ALL
# admin routes 403. Wrong keys are rate-limited (brute-force lockout).
ADMIN_KEY=change-me-to-a-long-random-string

# Global per-IP request throttle (requests/minute) across all /api routes.
# Lower this if a script/DoS hammers the API; the per-user limits still apply.
# GLOBAL_IP_RATE_PER_MIN=480

# HMAC key for one-shot session tokens. Defaults to BOT_TOKEN if unset.
# SESSION_SECRET=another-long-random-string

# Minimum FLAP coins per withdrawal request (integer). Default 10.
# MIN_WITHDRAW_FLAP=10

PORT=3000

# TON address players send funds to when topping up FLAP (shown in Wallet).
DEPOSIT_TON_ADDRESS=UQB...your-ton-address

# Where scores, ranks, FLAP balances and withdrawals are saved.
# On Railway: create a Volume, mount it at /data, then set:
DATA_FILE=/data/store.json

# ---------------------------------------------------------------------------
# DATA SAFETY (player balances, referrals, leaderboard)
# ---------------------------------------------------------------------------
# STRONGLY RECOMMENDED: Upstash Redis. Data then lives OUTSIDE the container,
# so no code change / redeploy can ever erase it.
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token

# Verify the setup AFTER each deploy (must return ok:true):
#   GET /internal/durability  (x-admin-key header)   or   npm run check:durable
# This performs a REAL write->read->delete round-trip against the live backend,
# so it catches wrong tokens / unreachable redis / read-only volumes too.

# How often an automatic rolling backup snapshot is taken (ms). Default 1h.
BACKUP_EVERY_MS=3600000
# How many rolling backups to keep (Redis + local files). Default 48.
BACKUP_KEEP=48
# How long a backup lives in Redis (seconds). Default 30 days.
BACKUP_TTL_SEC=2592000
# Extra save to storage even when the game is idle (ms). Default 5 min.
AUTOSAVE_MS=300000
# Refuse to save if the player count would fall below this fraction of the
# highest count we have seen (protection against a wipe). Default 0.5.
SHRINK_GUARD_RATIO=0.5
